\chapter{Heterogeneous Reaction}\label{ch:heterogeneous}

This chapter applies the inference framework to a reaction involving two
electron transfers coupled by an irreversible heterogeneous chemical
step. The parameter space grows from three to seven dimensions.
Because the methodology has been established, we focus on
what is new: the reaction scheme, the coupled PDE system, the reordering
that yields a pentadiagonal linear system, and the inference results. A
more detailed treatment of surface processes with explicit
surface concentrations and adsorption/desorption kinetics is covered
in \cref{ch:adsorption}.

\section{Reaction Scheme}\label{sec:het-scheme}

We consider the $\mathrm{EC}_{\mathrm{irr,het}}\mathrm{E}$
mechanism~\cite{compton2014understanding}:
\begin{equation}\label{eq:het-scheme}
  \begin{gathered}
    \mathrm{A} + \mathrm{e}^{-} \rightleftharpoons \mathrm{B}, \\
    \mathrm{B} \xrightarrow{k_{\mathrm{het}}} \mathrm{C}, \\
    \mathrm{C} + \mathrm{e}^{-} \rightleftharpoons \mathrm{D}.
  \end{gathered}
\end{equation}
Species~$\mathrm{A}$ is reduced to~$\mathrm{B}$; $\mathrm{B}$ is
converted irreversibly to~$\mathrm{C}$ by a surface-catalysed process
with rate constant $k_{\mathrm{het}}$~($\mathrm{m\,s^{-1}}$).
$\mathrm{C}$ undergoes a second electron transfer to form~$\mathrm{D}$.
The chemical step occurs only at the electrode surface, so bulk transport
of all four species is governed by Fick's second law. This mechanism
arises, for example, in the electroreduction of halonitroaromatic
compounds in aprotic media~\cite{compton2014understanding}.

\begin{figure}
  \centering
  \input{figures/heterogeneous-reaction.tex}
  \caption[Schematic of the
  $\mathrm{EC}_{\mathrm{irr,het}}\mathrm{E}$ reaction]{Schematic of
    the $\mathrm{EC}_{\mathrm{irr,het}}\mathrm{E}$ reaction mechanism.
    Two electrochemical steps are coupled by an irreversible
  heterogeneous chemical step at the electrode surface.}
  \label{fig:het-schematic}
\end{figure}

Each electron transfer has its own $\alpha^{(i)}$, $K_0^{(i)}$, and
$\theta_f^{(i)}$. Together with the dimensionless heterogeneous rate
constant $K_{\mathrm{het}}$, the parameter vector is
\begin{equation}\label{eq:het-params}
  \phi = \bigl(\alpha^{(1)},\, K_0^{(1)},\, \theta_f^{(1)},\,
    \alpha^{(2)},\, K_0^{(2)},\, \theta_f^{(2)},\,
  K_{\mathrm{het}}\bigr) \in \mathbb{R}^7.
\end{equation}

\section{Mathematical Description}\label{sec:het-maths}

Under the equal-diffusion assumption ($d_j = 1$), each species satisfies
the dimensionless diffusion equation~\cref{eq:nd_diffusion}. Unlike the
single-electron case, the heterogeneous step breaks stoichiometric
conservation at the surface since $\mathrm{B}$ is consumed both by
oxidation to~$\mathrm{A}$ and by conversion to~$\mathrm{C}$. Thus the
system cannot be reduced to a single PDE and all four equations must be
solved.

\subsection{Initial and Boundary Conditions}

We assume only $\mathrm{A}$ is present initially:
$C_\mathrm{A}(0,X) = 1$, $C_\mathrm{B} = C_\mathrm{C} = C_\mathrm{D}
= 0$. These values are also imposed at $X = X_{\max}$.

We apply the scalings of \cref{tab:normalisation}, defining
$K_{\mathrm{red}}^{(i)}$ and $K_{\mathrm{ox}}^{(i)}$ for each electron
transfer as in~\cref{eq:kred_kox}, and the dimensionless heterogeneous
rate constant as $K_{\mathrm{het}} = k_{\mathrm{het}}\, r_e /
D_\mathrm{A}$. The boundary conditions at $X = 0$
are~\cite{compton2014understanding}
\begin{equation}\label{eq:het-bc-nondim}
  \begin{aligned}
    \left(\frac{\partial C_\mathrm{A}}{\partial X}\right)_{X=0}
    &= K_{\mathrm{red}}^{(1)}\, C_\mathrm{A}
    - K_{\mathrm{ox}}^{(1)}\, C_\mathrm{B}, \\[4pt]
    \left(\frac{\partial C_\mathrm{B}}{\partial X}\right)_{X=0}
    &= -K_{\mathrm{red}}^{(1)}\, C_\mathrm{A}
    + \bigl(K_{\mathrm{ox}}^{(1)} + K_{\mathrm{het}}\bigr)\,
    C_\mathrm{B}, \\[4pt]
    \left(\frac{\partial C_\mathrm{C}}{\partial X}\right)_{X=0}
    &= K_{\mathrm{red}}^{(2)}\, C_\mathrm{C}
    - K_{\mathrm{ox}}^{(2)}\, C_\mathrm{D}
    - K_{\mathrm{het}}\, C_\mathrm{B}, \\[4pt]
    \left(\frac{\partial C_\mathrm{D}}{\partial X}\right)_{X=0}
    &= -K_{\mathrm{red}}^{(2)}\, C_\mathrm{C}
    + K_{\mathrm{ox}}^{(2)}\, C_\mathrm{D},
  \end{aligned}
\end{equation}
where all surface concentrations are evaluated at $(T, 0)$. The
$K_{\mathrm{het}}$ term appears only in the $\mathrm{B}$ and $\mathrm{C}$
equations and is the sole coupling between the two electron-transfer
subsystems.

\subsection{Current}

The measured current includes contributions from both electrochemical
steps and the heterogeneous conversion~\cite{compton2014understanding}:
\begin{equation}\label{eq:het-current}
  J = -\left\{
    \left(\frac{\partial C_\mathrm{A}}{\partial X}\right)_{X=0}
    + \left(\frac{\partial C_\mathrm{C}}{\partial X}\right)_{X=0}
    + K_{\mathrm{het}}\, C_\mathrm{B}(T, 0)
  \right\}.
\end{equation}

\section{Pentadiagonal System}\label{sec:het-disc}

Each species is discretised on the same exponentially expanding mesh of
\cref{sec:fd-scheme}. The four species are coupled only through the
boundary conditions at $X = 0$. A species-by-species ordering of
the unknowns places the surface coupling entries $n$ positions
from the main diagonal.

The bandwidth can be reduced by interleaving the unknowns $A$ with
$B$ and $C$ with $D$. Additionally, $\mathrm{A}$ and $\mathrm{B}$ are
indexed from $X_{n-1}$ down to $X_0$, while $\mathrm{C}$ and
$\mathrm{D}$ run from $X_0$ to $X_{n-1}$:
\begin{equation}\label{eq:het-ordering}
  \mathbf{C} = \bigl(
    C_\mathrm{A}^{n-1}, C_\mathrm{B}^{n-1}, \ldots,
    C_\mathrm{A}^{0}, C_\mathrm{B}^{0},\,
    C_\mathrm{C}^{0}, C_\mathrm{D}^{0}, \ldots,
    C_\mathrm{C}^{n-1}, C_\mathrm{D}^{n-1}
  \bigr)^T.
\end{equation}

The $K_{\mathrm{het}}$ coupling between $C_\mathrm{B}^0$ and
$C_\mathrm{C}^0$ is now adjacent with other surface coupling within
two positions of the main diagonal. The result is a pentadiagonal
system solved in $O(n)$ per timestep. As noted in
\cref{sec:pentadiagonal}, its transpose is again pentadiagonal, so
the adjoint system is solved at the same cost.

\section{Inference Setup}\label{sec:het-inference}

The inference workflow follows \cref{sec:workflow}. The seven parameters
are transformed: $\alpha^{(i)} \mapsto \operatorname{logit}(\alpha^{(i)})$,
$K_0^{(i)} \mapsto \log K_0^{(i)}$, $K_{\mathrm{het}} \mapsto \log
K_{\mathrm{het}}$, with $\theta_f^{(i)}$ untransformed.

Synthetic data is generated at $$\phi^* = ( 0.5,\,15.0,\,0.2,\,
0.5,\, 6.0,\, 0.4,\, 25.0)$$ with $\sigma = 1000$, $\eta = 0.02$,
$\theta_i = 10.0$ and $\theta_v = 10.0$.
Under equal runtime
budgets this gives $2{,}400$ NUTS samples and $40{,}000$ RWMH samples.

\section{Results}\label{sec:het-results}

\subsection{Solver Verification}\label{sec:het-verification}

We verify the solver in two limiting
cases~\cite{prieto1997electrode}. When $K_{\mathrm{het}} \to 0$ the
chemical step is inactive and the system reduces to a single electron
transfer, whose peak current and potential match
\cref{eq:irreversible-peak}. When $K_{\mathrm{het}} \to \infty$ with
$K_0^{(2)} \to \infty$, every molecule of $\mathrm{B}$ is instantly
converted to $\mathrm{C}$ and reduced to $\mathrm{D}$, giving a
two-electron transfer with twice the peak current.
\Cref{fig:het-analytical} confirms both limits.

\begin{figure}[ht]
  \centering
  \includegraphics[width=0.6\textwidth]{figures/7-analytical.png}
  \caption[Verification of the heterogeneous solver against analytical
  limits]{Verification in two limiting regimes.
    Blue: $K_{\mathrm{het}} \to 0$, recovering the single-electron
    voltammogram. Orange: $K_{\mathrm{het}} \to \infty$ with
    $K_0^{(2)} \to \infty$, recovering a two-electron transfer. Red
    dashed lines indicate analytical peak current and potential
    from~\cref{eq:irreversible-peak} (scaled by two for the
  two-electron case)~\cite{prieto1997electrode}.}
  \label{fig:het-analytical}
\end{figure}

\subsection{Posterior Distributions}\label{sec:het-posteriors}

\Cref{fig:het-corner} shows selected pairwise marginal posteriors.
Both samplers recover the true values across all seven dimensions;
the diagonal overlays both marginals, while the
off-diagonal shows the NUTS joint structure alone for clarity.
Within the first electron transfer, $\alpha^{(1)}$ and $K_0^{(1)}$ are
negatively correlated, which differs from what we saw in the previous
chapter. From~\cref{eq:kred_kox}, an increase in $\alpha^{(1)}$ can be
compensated by a decrease in $K_0^{(1)}$ to leave
$K_{\mathrm{red}}^{(1)}$ near the cathodic peak approximately
unchanged; the sign of this trade-off depends on which side of
$\theta_f^{(1)}$ the peak sits. The two transfer coefficients
$\alpha^{(1)}$ and $\alpha^{(2)}$ are also correlated, propagated
through the shared surface coupling between $C_\mathrm{B}$ and
$C_\mathrm{C}$. Between electron transfers, the rate constants
$K_0^{(1)}$ and $K_0^{(2)}$ correlate negatively: an increase in the
rate of the first transfer can be offset by a decrease in the second,
since both contribute additively to the measured current
in~\cref{eq:het-current}.

The $K_{\mathrm{het}}$ marginal has a width comparable to that of
$K_0^{(2)}$, despite its larger value ($K_{\mathrm{het}} = 25$ vs
$K_0^{(2)} = 6$). By contrast, $K_0^{(1)}$
has a width of approximately $5$ units around its true value of $15$.
The heterogeneous rate constant thus remains identifiable even at larger
values, reflecting the distinct contribution of the chemical step in the
current.

\begin{figure}[p]
  \centering
  \includegraphics[width=1.0\textwidth]{figures/7-corner.png}
  \caption[Selected pairwise posteriors for the heterogeneous
  reaction]{Selected pairwise marginal posteriors. Diagonal:
    histograms of NUTS (blue) with $2{,}400$ samples
    and RWMH (orange) with $40{,}000$ samples. Off-diagonal: NUTS KDE
    contours. Dashed lines indicate true values. Formal potentials
  omitted for clarity.}
  \label{fig:het-corner}
\end{figure}

\Cref{fig:het-current-fit} shows the posterior-mean voltammogram and
95\% credible interval against the noisy data; the fit confirms that
the sampled posteriors are consistent with the observations.

\begin{figure}[ht]
  \begin{center}
    \includegraphics[width=1.0\textwidth]{figures/7-current-fit.png}
  \end{center}
  \caption[Posterior-mean current fit for the heterogeneous
  reaction]{Posterior-mean voltammogram (solid) with 95\% credible
    interval (shaded) of the NUTS posterior against the true current
    (dashed) and noisy data
    (points). The mean is indistinguishable from the true current with
  tight credibility intervals.}
  \label{fig:het-current-fit}
\end{figure}

\subsection{$\hat{R}$ Convergence and Sampling Efficiency}\label{sec:het-ess}

\Cref{fig:het-gr} shows $\hat{R}$ over sampling budget. NUTS remains
below the $1.01$ threshold throughout. RWMH
requires a substantially larger fraction of the budget to converge
than in the three-parameter case: the
$\hat{R}$ curves for $\theta_f^{(1)}$ and $K_0^{(1)}$ remain above
$1.01$ for the majority of the run.

\begin{figure}[ht]
  \centering
  \includegraphics[width=1.00\textwidth]{figures/7-gr.png}
  \caption[$\hat{R}$ for the heterogeneous reaction]{$\hat{R}$ as a
    function of sample proportion for NUTS (blue) and RWMH (orange)
    for each parameter of the ECirrhetE mechanism. NUTS remains below
    the $1.01$ threshold throughout; RWMH requires a larger fraction
    of the budget to converge than in the three-parameter case
    (\cref{fig:electron-gr}). The dashed line marks the $\hat{R} = 1.01$
  convergence threshold~\cite{vehtari2021rank}.}
  \label{fig:het-gr}
\end{figure}

NUTS achieves $2.1$--$2.8\times$ the ESS of RWMH across all seven
parameters (\Cref{tab:het-ess}), up from $1.6$--$1.9\times$ in the
electron reaction. As the number of parameters grows, a random-walk
proposal is increasingly likely to land in low-density regions and
must shrink to maintain a reasonable acceptance rate, increasing the
within-chain correlation. The gradient-informed proposals do not
suffer from this issue.

\begin{table}[ht]
  \centering
  \begin{tabular}{lccccccc}
    \hline
    & $\alpha^{(1)}$ & $K_0^{(1)}$ & $\theta_f^{(1)}$
    & $\alpha^{(2)}$ & $K_0^{(2)}$ & $\theta_f^{(2)}$
    & $K_{\mathrm{het}}$ \\
    \hline
    NUTS & 3108.0 & 2845.3 & 2841.7 & 2968.4 & 3056.3 & 3869.3 & 3843.7 \\
    RWMH & 1347.0 & 1227.8 & 1234.4 & 1408.8 & 1415.0 & 1372.3 & 1404.1 \\
    Ratio & 2.3 & 2.3 & 2.3 & 2.1 & 2.2 & 2.8 & 2.7 \\
    \hline
  \end{tabular}
  \caption[ESS for the heterogeneous reaction]{Effective sample size
    for each parameter at equal runtime budget, with NUTS/RWMH
  ratio.}
  \label{tab:het-ess}
\end{table}

\section{AC Voltammetry}\label{sec:het-ac}

In AC voltammetry a sinusoidal perturbation is superimposed on the DC
ramp:
\begin{equation}\label{eq:ac-potential}
  \theta(T) = \theta_{\mathrm{DC}}(T) + \delta\theta\,
  \sin(2\pi f\, T),
\end{equation}
where $\delta\theta$ is the amplitude and $f$ the frequency. The
oscillatory component of the current probes the electrode kinetics on a
faster timescale than the DC sweep alone, providing additional
information about the rate constants and transfer
coefficients. Gavaghan et~al.~\cite{gavaghan2018bayesian} demonstrated
this for the single-electron reaction; we extend the comparison to the
heterogeneous case.

We use $\delta\theta = 0.5$ and $128$ oscillations over the
experiment, giving $f = 2 \times 128\pi / T_{\max}$. The $10{,}000$
timesteps from the convergence study resolve the sinusoidal waveform
without further refinement. No changes are required to the forward
solver or inference workflow.

\Cref{fig:ac-voltam} shows the resulting voltammogram at the true
parameters; the oscillations are largest near the formal potentials,
where the Butler--Volmer kinetics are most sensitive to potential changes.

\begin{figure}[ht]
  \centering
  \includegraphics[width=0.7\textwidth]{figures/7-ac-voltam.png}
  \caption[AC voltammogram for the heterogeneous reaction]{AC
    voltammogram at the true parameter values ($\delta\theta = 0.5$,
    $128$ oscillations). Oscillations are largest near the formal
  potentials of the two electron transfers.}
  \label{fig:ac-voltam}
\end{figure}

\Cref{fig:ac-dc-comparison} overlays the marginal posteriors from DC and
AC data using NUTS. The AC posteriors are narrower for all parameters,
with modes more closely aligned with the true values, consistent with
Gavaghan et~al.~\cite{gavaghan2018bayesian}. The oscillatory
perturbation probes the electrode kinetics at the frequency~$f$, a
timescale independent of the DC sweep rate~$\sigma$, providing
additional constraint on the rate constants and transfer coefficients.
The DC posteriors are adequate for describing the current, as the
results of \cref{fig:het-current-fit} confirm, but the AC data
tighten them further.

\begin{figure}[ht]
  \centering
  \includegraphics[width=0.95\textwidth]{figures/7-ac-hist-comparison.png}
  \caption[AC vs DC posterior comparison for the heterogeneous
  reaction]{Marginal posteriors under DC (blue) and AC (orange)
    voltammetry. Dashed lines indicate true values. AC posteriors are
    narrower for all parameters, reflecting additional kinetic
  information in the oscillatory current.}
  \label{fig:ac-dc-comparison}
\end{figure}
